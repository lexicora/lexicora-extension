{
  "hooks": {
    "WorktreeCreate": [
      {
        "type": "command",
        "command": "bash -c '\nNAME=$(jq -r .name < /dev/stdin)\nDIR=\".claude/worktrees/${NAME//\\//-}\"  # Replace / with - for dir\nBRANCH=\"$NAME\"  # Use the name as-is for the branch\n\ngit worktree add \"$DIR\" -b \"$BRANCH\" origin/HEAD 2>&1 >&2\necho \"$DIR\"\n'"
      }
    ]
  }
}
