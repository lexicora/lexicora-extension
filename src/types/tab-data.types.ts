/**
 * Contains only necessary tab data for messaging purposes
 */
export type TabData = {
  tabId: number | undefined;
  windowId: number;
  url?: string;
  title?: string;
  //favIconUrl?: string;
  // Add other properties as needed
};
